<template>
  <!-- Address -->
  <div class="pb-4 border-b border-zinc-700">
    <div class="flex justify-between">
      <h6 class="text-white">{{ $t("labels.residence") }}</h6>
      <h6>{{ $t("profile.country") }}</h6>
    </div>

    <div class="flex justify-between">
      <h6 class="text-white">{{ $t("labels.city") }}</h6>
      <h6>{{ $t("profile.city") }}</h6>
    </div>
  </div>
</template>
